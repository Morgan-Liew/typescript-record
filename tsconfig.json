{
/*
  tsconfig.json 是 ts编译器的配置文件，ts编译器可以根据它的信息对代码进行编译
    “include” 用来指定哪些ts文件需要被编译
    "exclude" 不需要被编译的文件目录
    路径： ** 表示任意目录
          *  表示任意文件
*/

  "compilerOptions": {
    "target": "es5",
//    "types": ["node"]
    "typeRoots": ["./typings"]
  },
  "include":[
    "./src/**/*"
  ],
  "exclude":[
    "./src/hello/**/*"
  ]
}
